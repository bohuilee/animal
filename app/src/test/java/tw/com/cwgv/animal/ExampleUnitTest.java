package tw.com.bobolee.animal;

import org.junit.Test;

import java.util.ArrayList;

import static org.junit.Assert.*;

/**
 * To work on unit tests, switch the Test Artifact in the Build Variants view.
 */
public class ExampleUnitTest {
    private ArrayList<Integer> numberOfDistinctValuesInPath = new ArrayList<>();
    @Test
    public void addition_isCorrect() throws Exception {
        assertEquals(4, 2 + 2);
        numberOfDistinctValuesInPath.toString()ing();
    }

    private class Tree {
        public int x;
        public Tree l;
        public Tree r;
    }

    @Test
    public void test() throws Exception {
        int[] A = new int[]{1, 4, -1, 3, 2};
        System.out.print(test2(A, new ArrayList<Integer>()));
    }

    private int test2(int[] A, ArrayList<Integer> b) {
        if(!b.contains(1)) {
            b.si
        }
    }
}